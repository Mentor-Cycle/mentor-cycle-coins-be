version: '3.6'

services:
  my-mongodb:
    image: mongo
    container_name: mentor-cycle-mongodb
    ports:
      - 27017:27017
    environment:
      - MONGO_INITDB_DATABASE=mentor-cycle
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=admin
    volumes:
      # seeding scripts
      - ./mongo-entrypoint:/docker-entrypoint-initdb.d
      # named volumes
      - mongodb:/data/db
      - mongoconfig:/data/configdb
    networks:
      - mongo_net
volumes:
  mongodb:
  mongoconfig:

networks:
  mongo_net:
    driver: bridge
